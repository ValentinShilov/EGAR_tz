spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: egar
    password: egar
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate